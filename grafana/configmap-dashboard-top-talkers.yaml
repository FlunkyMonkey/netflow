apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-top-talkers
  namespace: netflow
data:
  top-talkers.json: |
    {
      "title": "Top Talkers",
      "uid": "netflow-top-talkers",
      "schemaVersion": 38,
      "version": 1,
      "refresh": "1m",
      "time": { "from": "now-1h", "to": "now" },
      "panels": [
        {
          "id": 1,
          "title": "Top Talkers by Bytes",
          "type": "barchart",
          "gridPos": { "h": 20, "w": 24, "x": 0, "y": 0 },
          "options": {
            "orientation": "horizontal",
            "legend": { "displayMode": "list", "placement": "bottom" }
          },
          "fieldConfig": {
            "defaults": {
              "color": { "fixedColor": "#c49a6c", "mode": "fixed" },
              "unit": "bytes"
            }
          },
          "targets": [
            {
              "datasource": { "type": "influxdb", "uid": "PE1145B8E425975A4" },
              "query": "from(bucket: \"netflow\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"netflow\" and r._field == \"in_bytes\")\n  |> group(columns: [\"src\", \"src_host\"])\n  |> sum()\n  |> sort(columns: [\"_value\"], desc: true)\n  |> limit(n: 20)",
              "refId": "A"
            }
          ]
        }
      ],
      "templating": {
        "list": [
          {
            "name": "datasource",
            "type": "datasource",
            "pluginId": "influxdb",
            "current": { "text": "InfluxDB-Netflow", "value": "PE1145B8E425975A4" }
          }
        ]
      }
    }
